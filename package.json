"scripts": {
  "dev": "vite --port=3000 --host=0.0.0.0",
  "build": "vite build",
  "preview": "vite preview",
  "start": "npm run build && vite preview --host 0.0.0.0 --port 8080",
  "clean": "rm -rf dist",
  "lint": "tsc --noEmit"
},